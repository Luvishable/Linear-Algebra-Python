# Hadamard Multiplication

import numpy as np

# It's also called element-wise multiplication
# Hadamard multiplication is only valid if two vectors have the same size of elements(same dimensionality)

# create vectors
v1 = [1, 3, 5]
v2 = [3, 4, 2]

v3 = np.multiply(v1, v2)


